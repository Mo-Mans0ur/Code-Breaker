public class test {
    public static void main(String[] args) {
        /*
        for (int i = 0; i < word.length; i++) {
            System.out.println(word[i]);
            //int indexOfString = word.indexOf(i);
            System.out.println(alphabet2.charAt(word[i]-1));
        }
        */

         /*
        String decodedString = "";
        String words = "ben";
        int[] number = new int[words.length()];

        int[] decoderarray = new int[number.length];
        for (int i = 0; i < decoderarray.length; i++) {

        }
        for (int i = 0; i < decoderarray.length; i++) {
            System.out.println(words.charAt(decoderarray[i]));
        }

        return decodedString;

         */

        /*
        for (int i = 0; i < indexes.length; i++) {
            System.out.println( alphabet2.charAt());
        }

         */
    }
}
